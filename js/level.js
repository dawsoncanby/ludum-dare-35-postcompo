function createLevel() {
  var level = new Object();

  // holds tree branches, pockets of air flow
  level.obstacles = new Array();

  // generate a block of obstacles
  // difficulty will be from 0 to 1
  // yStart tells the generator how far up to start generating
  level.generateObstacles = function(difficulty, yStart) {

  }

  return level;
}
